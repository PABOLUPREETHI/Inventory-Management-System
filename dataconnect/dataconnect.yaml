specVersion: "v1beta"
serviceId: "emart-inventory"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "emart-inventory-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
